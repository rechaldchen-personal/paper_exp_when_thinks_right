# Analysis environment (CPU) — pinned for reproducibility.
#
# Versions are pinned because run 1 was analyzed with an unpinned scipy whose
# `wilcoxon(method="auto")` heuristic differs across releases: the same traces
# gave p=0.0625 on the GPU box and p=0.0339 under scipy 1.7.3 locally.
# 03_analyze.py no longer depends on that heuristic (it computes the exact
# signed-rank null itself), but pinning keeps figures and summaries stable.
#
# Setup:  ./setup_env.sh      Verify:  venv/bin/python verify_env.py
# Tested against Python 3.9.6 (macOS system python3).

numpy==1.26.4
scipy==1.13.1
matplotlib==3.9.4
pandas==2.2.3

# GPU-only dependencies — install on the GPU box, not needed for analysis:
#   pip install torch transformers datasets
#   pip install -e ./jacobian-lens     # provides `jlens`
