# Core dependencies (CPU-compatible, installed)
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
scikit-learn>=1.0.0
pandas>=1.3.0
tqdm>=4.60.0
requests>=2.25.0

# GPU dependencies (install with GPU environment)
# torch>=1.12.0  # Install with CUDA support when on GPU
# transformers>=5.5.0  # Install with GPU environment
# datasets>=2.0.0  # Install with GPU environment

# Optional (for Jupyter if needed)
# jupyter>=1.0.0
# notebook>=6.0.0
